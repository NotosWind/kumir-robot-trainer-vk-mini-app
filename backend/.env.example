# VK Mini App secret (from app settings) used to verify launch-param signatures
VK_APP_SECRET=replace-me
# SQLAlchemy URL; default is a local SQLite file
DATABASE_URL=sqlite:///./kumir.db
# Set to 1 for local development without VK (auth returns VK_DEV_USER_ID)
VK_SKIP_AUTH=0
VK_DEV_USER_ID=1
